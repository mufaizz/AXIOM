# AI Math Visualization Platform

An AI-powered browser-based Math Visualization Platform that converts natural
language math questions into interactive, animated, step-by-step visual explanations.

## Quickstart

```bash
./setup.sh
cd math-viz-platform
cp .env.example .env
docker compose -f docker/docker-compose.yml up --build

